---
# Extra packages to install on all nodes before k3s provisioning
extra_packages:
  - open-iscsi
  - nfs-common

# Disable i915 GPU modesetting on headless servers to prevent
# EDID polling spam and unnecessary CPU usage
disable_i915_modesetting: true
