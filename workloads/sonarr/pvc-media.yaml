apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: sonarr-media
  resources:
    requests:
      storage: 10Ti
