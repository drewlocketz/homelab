traefik:
  service:
    annotations:
      metallb.universe.tf/loadBalancerIPs: "10.0.4.240"

  ports:
    web:
      redirectTo:
        port: websecure
    websecure:
      tls:
        enabled: true
