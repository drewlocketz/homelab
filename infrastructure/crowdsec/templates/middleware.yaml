apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: crowdsec-bouncer
  namespace: crowdsec
spec:
  plugin:
    crowdsec-bouncer:
      enabled: true
      crowdsecMode: stream
      crowdsecLapiHost: crowdsec-service.crowdsec.svc.cluster.local:8080
      crowdsecLapiKey: "0MjzFJRWdyqa/GIvT47pXil67UhmRZ58l63TGBFGrkM"
      crowdsecLapiScheme: http
      forwardedHeadersTrustedIps:
        - "10.42.0.0/16"
